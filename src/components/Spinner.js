import styles from "../assets/layouts/Spinner.module.css";

export default function Spinner() {
    return (
        <main className={styles.spinnerContainer}>
            <div></div>
        </main>
    )
}